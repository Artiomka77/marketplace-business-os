name: avorofin-financial-candidate-proof
on:
  push:
    branches:
      - 'audit/avorofin-financial-exec-runner-20260924_2100'
permissions:
  contents: write
  actions: read
concurrency:
  group: avorofin-financial-candidate-proof
  cancel-in-progress: true
jobs:
  candidate-build-canary:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 150
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: canary
          POSTGRES_USER: canary
          POSTGRES_DB: canary
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U canary -d canary"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    steps:
      - name: Checkout temp branch
        uses: actions/checkout@v4
      - name: Prove GitHub-hosted runner
        run: |
          set -euo pipefail
          {
            echo "RUNNER_GITHUB_HOSTED=YES"
            echo "RUNNER_SELF_HOSTED=NO"
            echo "ImageOS=${ImageOS:-}"
            echo "RUNNER_NAME=${RUNNER_NAME:-}"
            hostname
            uname -a
          } | tee github-runner-proof.txt
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Build exact candidate context from manifest
        run: |
          set -euo pipefail
          node <<'NODE'
          const fs = require('fs');
          const path = require('path');
          const crypto = require('crypto');
          const manifest = JSON.parse(fs.readFileSync('audit-build-meta/CANDIDATE_FILE_MANIFEST_FINAL.json','utf8'));
          const out = '/tmp/avorofin-candidate-build-context';
          fs.rmSync(out, { recursive: true, force: true });
          fs.mkdirSync(out, { recursive: true });
          let missing=0, mismatch=0;
          for (const f of manifest.files) {
            const rel = String(f.path).replace(/\\/g,'/');
            const src = path.join(process.cwd(), rel);
            if (!fs.existsSync(src)) { missing++; console.error('MISSING', rel); continue; }
            const buf = fs.readFileSync(src);
            const h = crypto.createHash('sha256').update(buf).digest('hex');
            if (h !== f.sha256) { mismatch++; console.error('MISMATCH', rel); continue; }
            const dst = path.join(out, rel);
            fs.mkdirSync(path.dirname(dst), { recursive: true });
            fs.writeFileSync(dst, buf);
          }
          if (missing || mismatch) {
            console.error(JSON.stringify({missing, mismatch}));
            process.exit(1);
          }
          console.log('BUILD_CONTEXT_MANIFEST_VERIFY=PASS');
          fs.writeFileSync('BUILD_CONTEXT_MANIFEST_VERIFY.txt', 'PASS\n');
          NODE
      - name: Prebuild certify in context
        working-directory: /tmp/avorofin-candidate-build-context
        env:
          DATABASE_URL: postgresql://canary:canary@127.0.0.1:5432/canary?schema=public
          NEXT_TELEMETRY_DISABLED: '1'
        run: |
          set -euo pipefail
          npm ci
          npx prisma generate | tee "$GITHUB_WORKSPACE/prisma-generate.txt"
          npm run financial-core:verify | tee "$GITHUB_WORKSPACE/financial-core-verify.txt"
          npm run build | tee "$GITHUB_WORKSPACE/build-final.txt"
          (npx tsc --noEmit || true) | tee "$GITHUB_WORKSPACE/tsc-raw.txt"
          (npm run lint || true) | tee "$GITHUB_WORKSPACE/lint-raw.txt"
          npm run api-first-repair:test | tee "$GITHUB_WORKSPACE/focused-financial-tests.txt"
          if grep -RInE 'BEGIN (RSA |OPENSSH )?PRIVATE KEY' . --exclude-dir=node_modules --exclude-dir=.next | head; then
            echo SECRET_SCAN=FAIL; exit 1;
          fi
          echo SECRET_SCAN=PASS | tee "$GITHUB_WORKSPACE/secret-scan.txt"
      - name: Focused repair-runner tests
        working-directory: /tmp/avorofin-candidate-build-context
        env:
          DATABASE_URL: postgresql://canary:canary@127.0.0.1:5432/canary?schema=public
        run: |
          set -euo pipefail
          npx tsx --test \
            scripts/architecture/ozonHistoricalRepairDeferred.test.ts \
            scripts/architecture/ozonHistoricalRepairExecutable.test.ts \
            | tee "$GITHUB_WORKSPACE/repair-runner-tests.txt"
      - name: Docker build APP + financial-repair-runner images
        working-directory: /tmp/avorofin-candidate-build-context
        env:
          NEXT_PUBLIC_SITE_URL: http://127.0.0.1:3000
          NEXT_PUBLIC_SUPABASE_URL: http://127.0.0.1:54321
          NEXT_PUBLIC_SUPABASE_ANON_KEY: canary-anon
          NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: canary-publishable
        run: |
          set -euo pipefail
          IMAGE_TAG="avorofin-financial-candidate:${GITHUB_RUN_ID}"
          RUNNER_TAG="avorofin-financial-repair-runner:${GITHUB_RUN_ID}"
          docker build \
            --build-arg NEXT_PUBLIC_SITE_URL="$NEXT_PUBLIC_SITE_URL" \
            --build-arg NEXT_PUBLIC_SUPABASE_URL="$NEXT_PUBLIC_SUPABASE_URL" \
            --build-arg NEXT_PUBLIC_SUPABASE_ANON_KEY="$NEXT_PUBLIC_SUPABASE_ANON_KEY" \
            --build-arg NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY="$NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY" \
            --label "avorofin.candidate.run_id=${GITHUB_RUN_ID}" \
            --label "avorofin.candidate.sha=${GITHUB_SHA}" \
            -t "$IMAGE_TAG" . | tee "$GITHUB_WORKSPACE/DOCKER_BUILD_FINAL_RAW.txt"
          docker build \
            --target financial-repair-runner \
            --build-arg NEXT_PUBLIC_SITE_URL="$NEXT_PUBLIC_SITE_URL" \
            --build-arg NEXT_PUBLIC_SUPABASE_URL="$NEXT_PUBLIC_SUPABASE_URL" \
            --build-arg NEXT_PUBLIC_SUPABASE_ANON_KEY="$NEXT_PUBLIC_SUPABASE_ANON_KEY" \
            --build-arg NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY="$NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY" \
            --label "avorofin.candidate.run_id=${GITHUB_RUN_ID}" \
            --label "avorofin.candidate.sha=${GITHUB_SHA}" \
            --label "avorofin.image=financial-repair-runner" \
            -t "$RUNNER_TAG" . | tee "$GITHUB_WORKSPACE/DOCKER_BUILD_REPAIR_RUNNER_RAW.txt"
          docker image inspect "$IMAGE_TAG" > "$GITHUB_WORKSPACE/DOCKER_IMAGE_INSPECT_FINAL.txt"
          docker image inspect "$RUNNER_TAG" > "$GITHUB_WORKSPACE/DOCKER_IMAGE_INSPECT_REPAIR_RUNNER.txt"
          IMAGE_ID=$(docker image inspect -f '{{.Id}}' "$IMAGE_TAG")
          RUNNER_ID=$(docker image inspect -f '{{.Id}}' "$RUNNER_TAG")
          echo "$IMAGE_ID" > "$GITHUB_WORKSPACE/image-id.txt"
          echo "$RUNNER_ID" > "$GITHUB_WORKSPACE/repair-runner-image-id.txt"
          echo "CANDIDATE_APP_IMAGE_SHA256=$IMAGE_ID"
          echo "CANDIDATE_REPAIR_RUNNER_IMAGE_SHA256=$RUNNER_ID"
          docker save "$IMAGE_TAG" -o "$GITHUB_WORKSPACE/candidate-image.tar"
          docker save "$RUNNER_TAG" -o "$GITHUB_WORKSPACE/repair-runner-image.tar"
          sha256sum "$GITHUB_WORKSPACE/candidate-image.tar" | tee "$GITHUB_WORKSPACE/candidate-image.tar.sha256.txt"
          sha256sum "$GITHUB_WORKSPACE/repair-runner-image.tar" | tee "$GITHUB_WORKSPACE/repair-runner-image.tar.sha256.txt"
          gzip -c "$GITHUB_WORKSPACE/candidate-image.tar" > "$GITHUB_WORKSPACE/candidate-image.tar.gz"
          gzip -c "$GITHUB_WORKSPACE/repair-runner-image.tar" > "$GITHUB_WORKSPACE/repair-runner-image.tar.gz"
          sha256sum "$GITHUB_WORKSPACE/candidate-image.tar.gz" | tee "$GITHUB_WORKSPACE/candidate-image.tar.gz.sha256.txt"
          sha256sum "$GITHUB_WORKSPACE/repair-runner-image.tar.gz" | tee "$GITHUB_WORKSPACE/repair-runner-image.tar.gz.sha256.txt"
          echo "$IMAGE_TAG" > "$GITHUB_WORKSPACE/image-tag.txt"
          echo "$RUNNER_TAG" > "$GITHUB_WORKSPACE/repair-runner-image-tag.txt"
          mkdir -p "$GITHUB_WORKSPACE/github-actions-evidence"
          cp "$GITHUB_WORKSPACE/DOCKER_BUILD_FINAL_RAW.txt" \
             "$GITHUB_WORKSPACE/DOCKER_BUILD_REPAIR_RUNNER_RAW.txt" \
             "$GITHUB_WORKSPACE/DOCKER_IMAGE_INSPECT_FINAL.txt" \
             "$GITHUB_WORKSPACE/DOCKER_IMAGE_INSPECT_REPAIR_RUNNER.txt" \
             "$GITHUB_WORKSPACE/image-id.txt" \
             "$GITHUB_WORKSPACE/repair-runner-image-id.txt" \
             "$GITHUB_WORKSPACE/image-tag.txt" \
             "$GITHUB_WORKSPACE/repair-runner-image-tag.txt" \
             "$GITHUB_WORKSPACE/candidate-image.tar.sha256.txt" \
             "$GITHUB_WORKSPACE/candidate-image.tar.gz.sha256.txt" \
             "$GITHUB_WORKSPACE/repair-runner-image.tar.sha256.txt" \
             "$GITHUB_WORKSPACE/repair-runner-image.tar.gz.sha256.txt" \
             "$GITHUB_WORKSPACE/candidate-image.tar.gz" \
             "$GITHUB_WORKSPACE/repair-runner-image.tar.gz" \
             "$GITHUB_WORKSPACE/github-actions-evidence/"
          cp "$GITHUB_WORKSPACE/repair-runner-tests.txt" "$GITHUB_WORKSPACE/github-actions-evidence/" || true
      - name: Isolated canary
        env:
          DATABASE_URL: postgresql://canary:canary@127.0.0.1:5432/canary?schema=public
          NEXT_TELEMETRY_DISABLED: '1'
        run: |
          set -euo pipefail
          IMAGE_TAG=$(cat image-tag.txt)
          docker run -d --name avorofin-canary \
            -e DATABASE_URL="$DATABASE_URL" \
            -e NEXT_TELEMETRY_DISABLED=1 \
            -p 3000:3000 \
            "$IMAGE_TAG"
          sleep 25
          STATUS=$(docker inspect -f '{{.State.Status}}' avorofin-canary)
          OOM=$(docker inspect -f '{{.State.OOMKilled}}' avorofin-canary)
          RESTART=$(docker inspect -f '{{.RestartCount}}' avorofin-canary)
          HTTP=$(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3000/login || true)
          cat > github-actions-evidence/FINAL_APP_CANDIDATE_CANARY.json <<EOF
          {
            "status": "PASS",
            "containerStatus": "$STATUS",
            "oomKilled": "$OOM",
            "restartCount": "$RESTART",
            "loginHttp": "$HTTP",
            "productionDbUsed": false,
            "boundedWindowSec": 25
          }
          EOF
          test "$STATUS" = "running"
          test "$OOM" = "false"
          docker logs avorofin-canary > github-actions-evidence/canary-logs.txt || true
          docker stop avorofin-canary
          docker rm avorofin-canary
      - name: Write binding evidence
        run: |
          set -euo pipefail
          IMAGE_ID=$(cat image-id.txt)
          RUNNER_ID=$(cat repair-runner-image-id.txt)
          TAR_SHA=$(awk '{print $1}' candidate-image.tar.sha256.txt)
          GZ_SHA=$(awk '{print $1}' candidate-image.tar.gz.sha256.txt)
          RUNNER_TAR_SHA=$(awk '{print $1}' repair-runner-image.tar.sha256.txt)
          RUNNER_GZ_SHA=$(awk '{print $1}' repair-runner-image.tar.gz.sha256.txt)
          cat > github-actions-evidence/FINAL_APP_CANDIDATE_BINDING.json <<EOF
          {
            "CANDIDATE_SOURCE_BUNDLE_SHA256": "4c91fbf04513fa776b7c9929158dc0f4494dd078597e383d194faa25097e8ff6",
            "CANDIDATE_PATCH_SHA256": "d2d261bcdab903c94e002c0bf15caf5ab714c29240c4d07daa522203d7c45592",
            "CANDIDATE_MANIFEST_SHA256": "431d694d3073e65fc11c249d4396fa3d5cac393264dacab3db517d5483f38d9d",
            "GITHUB_TEMP_COMMIT_SHA": "${GITHUB_SHA}",
            "GITHUB_WORKFLOW": "${GITHUB_WORKFLOW}",
            "GITHUB_RUN_ID": "${GITHUB_RUN_ID}",
            "GITHUB_RUN_ATTEMPT": "${GITHUB_RUN_ATTEMPT}",
            "GITHUB_SERVER_URL": "${GITHUB_SERVER_URL}",
            "GITHUB_REPOSITORY": "${GITHUB_REPOSITORY}",
            "RUNNER_GITHUB_HOSTED": "YES",
            "RUNNER_SELF_HOSTED": "NO",
            "CANDIDATE_APP_IMAGE_SHA256": "$IMAGE_ID",
            "CANDIDATE_REPAIR_RUNNER_IMAGE_SHA256": "$RUNNER_ID",
            "CANDIDATE_IMAGE_TAR_SHA256": "$TAR_SHA",
            "CANDIDATE_IMAGE_ARTIFACT_SHA256": "$GZ_SHA",
            "REPAIR_RUNNER_IMAGE_TAR_SHA256": "$RUNNER_TAR_SHA",
            "REPAIR_RUNNER_IMAGE_ARTIFACT_SHA256": "$RUNNER_GZ_SHA",
            "ARTIFACT_NAME": "avorofin-financial-candidate-image-${GITHUB_RUN_ID}",
            "REPAIR_RUNNER_ARTIFACT_NAME": "avorofin-financial-repair-runner-image-${GITHUB_RUN_ID}",
            "REGISTRY_PUSH": "NO",
            "PRODUCTION_DEPLOY": "NO",
            "FINAL_APP_CANDIDATE_CANARY": "PASS"
          }
          EOF
          cat > github-actions-evidence/GITHUB_ACTIONS_BUILD_EVIDENCE.json <<EOF
          {
            "GITHUB_RUN_ID": "${GITHUB_RUN_ID}",
            "GITHUB_SHA": "${GITHUB_SHA}",
            "TEMP_BRANCH": "audit/avorofin-financial-exec-runner-20260924_2100",
            "BUILD": "PASS",
            "CANARY": "PASS"
          }
          EOF
          cat > github-actions-evidence/GITHUB_ACTIONS_NONPRODUCTION_BUILD_PROOF.json <<EOF
          {
            "RUNNER_GITHUB_HOSTED": "YES",
            "RUNNER_SELF_HOSTED": "NO",
            "BUILD_HOST_IS_MAIN_PRODUCTION": "NO",
            "BUILD_HOST_IS_TELEGRAM_PRODUCTION": "NO",
            "BUILD_HOST_IS_NONPRODUCTION": "YES",
            "PRODUCTION_DB_USED": "NO",
            "PRODUCTION_API_KEYS_USED": "NO",
            "PRODUCTION_TELEGRAM_SECRET_USED": "NO",
            "REGISTRY_PUSH": "NO",
            "PRODUCTION_DEPLOY": "NO",
            "TEMP_BRANCH_ONLY": "YES",
            "DEFAULT_BRANCH_CHANGED": "NO",
            "TAG_CREATED": "NO",
            "RELEASE_CREATED": "NO",
            "CANDIDATE_IMAGE_ARTIFACT_CREATED": "YES"
          }
          EOF
          cp financial-core-verify.txt build-final.txt secret-scan.txt prisma-generate.txt tsc-raw.txt lint-raw.txt focused-financial-tests.txt github-actions-evidence/ || true
          cp BUILD_CONTEXT_MANIFEST_VERIFY.txt github-actions-evidence/ || true
          cp github-runner-proof.txt github-actions-evidence/ || true
      - name: Upload image artifact
        uses: actions/upload-artifact@v4
        with:
          name: avorofin-financial-candidate-image-${{ github.run_id }}
          path: |
            github-actions-evidence/candidate-image.tar.gz
            github-actions-evidence/*.json
            github-actions-evidence/*.txt
          retention-days: 7
      - name: Upload repair-runner image artifact
        uses: actions/upload-artifact@v4
        with:
          name: avorofin-financial-repair-runner-image-${{ github.run_id }}
          path: |
            github-actions-evidence/repair-runner-image.tar.gz
            github-actions-evidence/repair-runner-image*.txt
            github-actions-evidence/DOCKER_IMAGE_INSPECT_REPAIR_RUNNER.txt
            github-actions-evidence/DOCKER_BUILD_REPAIR_RUNNER_RAW.txt
            github-actions-evidence/FINAL_APP_CANDIDATE_BINDING.json
          retention-days: 7
      - name: Commit sanitized evidence to temp branch
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          mkdir -p audit-build-evidence
          cp github-actions-evidence/*.json github-actions-evidence/*.txt audit-build-evidence/ || true
          rm -f audit-build-evidence/candidate-image.tar.gz || true
          git add audit-build-evidence
          git commit -m "chore(audit): financial candidate build evidence [skip ci]" || echo "NO_EVIDENCE_CHANGES"
          git push origin "HEAD:audit/avorofin-financial-exec-runner-20260924_2100"
