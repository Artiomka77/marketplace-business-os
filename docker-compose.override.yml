services:
  app:
    network_mode: "host"
    networks: !reset []
    restart: unless-stopped
    environment:
      NODE_OPTIONS: "--max-old-space-size=4096 --dns-result-order=verbatim"

  caddy:
    network_mode: "host"
    networks: !reset []
    ports: !reset []
    restart: unless-stopped
